using System;
using UnityEngine;
using Unity.Mathematics;

public class BiomeGen : MonoBehaviour
{
    /*
    public static Color FinalNoise(float x, float y, float z, Vector2 origin, int size, float noiseScale, Texture2D biomeTexture)
    {
        float t;
        float r;

        return TerrainColor((int)t, (int)r, biomeTexture);
    }

    private static Color TerrainColor(int t, int r, Texture2D tex)
    {
        return tex.GetPixel(t,r);
    }
    */
}
